# TODO

- [x] Translate `lib/screens/coconut_about_page.dart` from Tagalog to English only.

- [ ] Update `lib/screens/history_page.dart` so history image thumbnail is tappable to view fullscreen.

- [ ] Fix Light/Dark mode: persist preference and apply it to `MaterialApp` theme in `lib/main.dart`.

- [ ] Adjust hard-coded light colors in pages affected (About/History/Settings) so dark mode works correctly.
- [ ] Run and manually test the app: About page English-only, fullscreen history viewer, theme toggle works & persists.

